/** @type {import('next').NextConfig} */
const nextConfig = {
  // server: {
  //   host: true,
  // },
  images: {
    domains: ["cdn.pixabay.com", "iso.500px.com"],
  },
};

module.exports = nextConfig;
